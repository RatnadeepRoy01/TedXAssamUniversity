import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode:false,
  images: {
    domains: ['www.jconline.com','communityrights.us','tse4.mm.bing.net','highlandfm.org.au','ik.imagekit.io', 'admandi.in'],
  },
};

export default nextConfig;
